-- Set programs that you use
local programs = {
    terminal    = "kitty",
    fileManager = "nautilus",
    menu        = "vicinae toggle",
    lock        = "qs ipc call lock lock",
    wswitcher   = "qs ipc call task_manager toggle"
}

return programs
